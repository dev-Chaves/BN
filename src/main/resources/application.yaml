quarkus:
  smallrye-jwt:
    enabled: true

mp:
  jwt:
    verify:
      publickey:
        location: publicKey.pem
      issuer: bn-api

smallrye:
  jwt:
    sign:
      key:
        location: privateKey.pem

  swagger-ui:
    always-include: true
